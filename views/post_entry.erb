<div>

<% m_format = "(" + entry_hash["mobile"][0, 3] + ") " + entry_hash["mobile"][3, 3] + "-" + entry_hash["mobile"][6, 4] %>
<% h_format = "(" + entry_hash["home"][0, 3] + ") " + entry_hash["home"][3, 3] + "-" + entry_hash["home"][6, 4] %>
<% w_format = "(" + entry_hash["work"][0, 3] + ") " + entry_hash["work"][3, 3] + "-" + entry_hash["work"][6, 4] %>

  <div>
    <h4>Details for <%= entry_hash["fname"] %> <%= entry_hash["lname"] %> have been entered into the phonebook!</h4>
    <p>Name: <%= entry_hash["lname"] %>, <%= entry_hash["fname"] %></p>
    <p>Address: <%= entry_hash["addr"] %>, <%= entry_hash["cit"] %> <%= entry_hash["state"] %> <%= entry_hash["zip"] %></p>
    <p>Mobile: <%= m_format %></p>
    <p>Home: <%= h_format %></p>
    <p>Work: <%= w_format %></p>
  </div>

  <hr>

  <div>
    <h4>What would you like to do next?</h4>
    <div class="padding">
      <p><a href = "/get_entry">Create a new phonebook entry</a></p>
      <p><a href = "/list_entries">View an existing phonebook entry</a></p>
      <p><a href = "/get_search">Update or delete an existing phonebook entry</a></p>
    </div>
  </div>

  <hr>

</div>